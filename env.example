# Copy to .env and adjust for your workspace.
# Used by Asset Bundle for deployment (e.g. workspace profile).

# Databricks CLI profile (must exist in ~/.databrickscfg)
# BUNDLE_TARGET=dev

# Override bundle variables (optional)
# catalog=cmegdemos_catalog
# schema=fraud_data
